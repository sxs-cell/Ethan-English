# Ethan英语 · 手机访问与部署指南

网站已改造为 **PWA（渐进式 Web 应用）**，支持手机浏览器访问、"添加到主屏幕"当 App 用、离线打开。以下三种方式，从易到难，任选其一。

---

## 方式一：局域网访问（最快，零成本，适合自己用）

**原理**：电脑当服务器，手机连同一 WiFi 就能打开。

**步骤**：
1. 电脑安装 Node.js 18+（https://nodejs.org/）
2. 双击 `start.bat`（Windows）或终端执行 `bash start.sh`（Mac/Linux）
3. 启动后窗口会显示「📱 手机访问地址」，例如 `http://192.168.1.5:3000`
4. 手机连接**与电脑相同的 WiFi**，浏览器输入该地址即可
5. 在手机浏览器菜单里点「添加到主屏幕」，即可像 App 一样全屏使用

> 局限：电脑需保持开机、网站需在局域网内。适合自己复习用，不适合公开分享。

---

## 方式二：GitHub Pages（免费，公开可访问，适合分享）

**步骤**：
1. 注册 GitHub 账号，新建一个仓库（如 `kaoyan-english`）
2. 把本目录（`kaoyan-site/`）内所有文件上传到仓库（可直接拖拽上传）
3. 仓库 → Settings → Pages → Source 选 `main` 分支 → 保存
4. 几分钟后得到网址 `https://你的用户名.github.io/kaoyan-english/`
5. 手机浏览器打开该网址即可，同样支持「添加到主屏幕」

> 优点：免费、HTTPS（PWA 完整生效）、24 小时在线。
> 注意：AI 批改在纯静态托管下需用「前端直连」模式（在 AI 配置里填 API Key，Key 保存在你自己手机，仅个人自用可接受）。

---

## 方式三：Vercel / Netlify（免费，操作类似，国内访问略慢）

1. 注册 Vercel（vercel.com）或 Netlify（netlify.com），用 GitHub 账号登录
2. 导入仓库 → 自动检测静态站点 → 无需配置直接 Deploy
3. 得到 `https://xxx.vercel.app` 网址

---

## 方式四：腾讯云 COS 静态网站托管（国内访问最快）

1. 开通腾讯云对象存储 COS，创建存储桶
2. 开启「静态网站」功能，设置默认首页 `index.html`
3. 上传本目录所有文件到存储桶
4. 绑定自定义域名（需备案）或使用默认加速域名
5. 手机浏览器访问域名即可

---

## 关于「AI 精批」的部署说明

| 部署方式 | AI 批改方案 |
|---------|------------|
| 局域网（server.js） | 后端代理模式：Key 填在 `start.bat`/`start.sh` 环境变量，前端留空，最安全 |
| GitHub Pages / Vercel / COS | 前端直连模式：在「AI 配置」填 Key，Key 存手机本地（个人自用） |

如需在公网也隐藏 Key，可将 `server.js` 部署到支持 Node 的平台（Railway / Render / Vercel Functions），或改用云函数作为代理。

---

## PWA 使用提示

- **添加到主屏幕**：iPhone 用 Safari 打开 → 分享按钮 →「添加到主屏幕」；安卓用 Chrome → 菜单 →「安装应用」/「添加到主屏幕」
- **离线使用**：首次联网打开后，核心内容（范文、词汇、技巧）会缓存，断网也能浏览
- **更新内容**：改动 `data.js` 后，把 `sw.js` 里的 `CACHE_NAME` 版本号（如 v1 → v2）加一，用户下次打开即自动更新
